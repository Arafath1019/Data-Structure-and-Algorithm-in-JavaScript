// Binary Heap

/*
    A binary heap is a binary tree with following properties:
        1. It's a complete tree
        2. A binary heap is either min heap or max heap.

*/

//Priority Queue

/*
    A priority queue is an extension of queue with the following properties:
        1. Every item has a priority associated with it.
        2. An element with high priority is dequeued before an element with low priority.
        3. If two elements have the same priority, then they are served according to their order in the queue.
*/

//Trie:

/*
    Trie is an efficient information retrieval data structure. Using trie, search complexities can be bought to optimal limit.
*/